{# empty Twig template #}
{% extends "portal.html.twig" %}
{% block title %}Index{% endblock %}

   {% block body %}
       {% for post in posts %}
    <li>
            <h2>{{post.title}}</h2>
           
            <p>{{post.text}}</p>
            <p class="readmore"><a href="/{{post.id}}">Continue Reading &raquo;</a></p>
    </li>
    {%endfor%}


	
</table>
{% endblock %}